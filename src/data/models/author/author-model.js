class AuthorModel {
    constructor(id, name, avatar, coursesCount) {
        this.id = id
        this.name = name
        this.avatar = avatar
        this.coursesCount = coursesCount
    }
}

export default AuthorModel